import React from "react";
import { View, Text } from "react-native";
import Layout from "./layout"; // Import the Layout component

const Plants: React.FC = () => {
  return (
    <Layout>
      <View>
        <Text>This is the Plants Screen</Text>
      </View>
    </Layout>
  );
};

export default Plants;